# 菜谱页面设计规范文档

## 一、技术栈

### UI库和工具
- **React**: 组件化框架
- **Tailwind CSS**: 原子化CSS框架（仅使用核心工具类）
- **lucide-react**: 图标库
  - 使用的图标: `Clock`, `ChefHat`, `Lightbulb`, `AlertCircle`

### 核心依赖
```javascript
import React, { useState } from 'react';
import { Clock, ChefHat, Lightbulb, AlertCircle } from 'lucide-react';
```

---

## 二、整体布局结构

### 页面层级（从外到内）

```
根容器 (min-h-screen)
├── 头部区域 (header)
│   ├── 菜名和标签
│   ├── 简介
│   └── 快速信息卡片（4个并排）
│
└── 主内容区域 (main)
    ├── 标签页导航
    └── 标签页内容
```

---

## 三、详细设计规范

### 3.1 根容器
```css
className="min-h-screen bg-gradient-to-br from-orange-50 via-white to-green-50"
```
- **最小高度**: 全屏高度
- **背景**: 从左上到右下的渐变，橙色→白色→绿色

---

### 3.2 头部区域

#### 外层容器
```css
className="bg-white shadow-sm"
```
- 白色背景，轻微阴影

#### 内容容器
```css
className="max-w-5xl mx-auto px-6 py-8"
```
- **最大宽度**: 5xl (约1024px)
- **水平居中**: mx-auto
- **内边距**: 左右24px，上下32px

#### 菜名
```css
className="text-4xl font-bold text-gray-900 mb-3"
```
- **字号**: 4xl (约36px)
- **字重**: bold
- **颜色**: 深灰色 (gray-900)
- **下边距**: 12px

#### 标签容器
```css
className="flex flex-wrap gap-2 mb-4"
```
- **布局**: 弹性布局，允许换行
- **间距**: 8px
- **下边距**: 16px

#### 单个标签
```css
className="px-3 py-1 bg-orange-100 text-orange-700 rounded-full text-sm font-medium"
```
- **内边距**: 水平12px，垂直4px
- **背景色**: 浅橙色
- **文字颜色**: 深橙色
- **形状**: 全圆角 (rounded-full)
- **字号**: sm (约14px)

#### 简介
```css
className="text-gray-600 text-lg leading-relaxed"
```
- **颜色**: 中灰色
- **字号**: lg (约18px)
- **行高**: 宽松 (leading-relaxed)

---

### 3.3 快速信息卡片（重点优化区域）

#### 网格容器（修改建议）
```css
原代码: className="grid grid-cols-1 md:grid-cols-4 gap-4 mt-6"
优化为: className="grid grid-cols-4 gap-3 mt-6"
```
- **布局**: 4列网格（始终4列，不响应式切换）
- **间距**: 12px
- **上边距**: 24px

#### 单个卡片
```css
className="bg-gradient-to-br from-blue-50 to-blue-100 rounded-xl p-4 border border-blue-200"
```
- **背景**: 渐变（左上到右下），浅色到稍深
- **圆角**: xl (约12px)
- **内边距**: 16px
- **边框**: 1px，对应颜色200号

**四种卡片颜色方案**:
1. **难度**: blue系（from-blue-50 to-blue-100, border-blue-200）
2. **准备时间**: green系（from-green-50 to-green-100, border-green-200）
3. **烹饪时间**: purple系（from-purple-50 to-purple-100, border-purple-200）
4. **总计时间**: amber系（from-amber-50 to-amber-100, border-amber-200）

#### 卡片内部结构
```html
<div class="卡片容器">
  <!-- 标题行 -->
  <div class="flex items-center gap-2 text-[color]-700 mb-1">
    <Icon class="w-5 h-5" />
    <span class="font-semibold">标题</span>
  </div>
  
  <!-- 数值 -->
  <p class="text-2xl font-bold text-[color]-900">数值</p>
</div>
```

---

### 3.4 主内容区域

#### 容器
```css
className="max-w-5xl mx-auto px-6 py-8"
```
- 与头部容器一致的宽度和边距

---

### 3.5 标签页导航

#### 外层容器
```css
className="bg-white rounded-t-xl shadow-sm border-b"
```
- **背景**: 白色
- **圆角**: 仅顶部圆角 xl
- **阴影**: 轻微
- **底部边框**: 1px

#### 按钮容器
```css
className="flex gap-1 p-2"
```
- **布局**: 弹性布局
- **间距**: 4px
- **内边距**: 8px

#### 单个按钮
```css
// 激活状态
className="flex-1 px-4 py-3 rounded-lg font-medium bg-orange-500 text-white shadow-md"

// 未激活状态
className="flex-1 px-4 py-3 rounded-lg font-medium bg-gray-50 text-gray-600 hover:bg-gray-100"
```
- **占据空间**: flex-1（均分）
- **内边距**: 水平16px，垂直12px
- **圆角**: lg (约8px)
- **过渡**: transition-all

**四个标签**:
1. 🥬 用料选材 (ingredients)
2. 👨‍🍳 烹饪步骤 (steps)
3. 💡 进阶技巧 (tips)
4. ⚠️ 注意事项 (notes)

---

### 3.6 标签页内容区

#### 容器
```css
className="bg-white rounded-b-xl shadow-sm p-8"
```
- **背景**: 白色
- **圆角**: 仅底部圆角 xl
- **阴影**: 轻微
- **内边距**: 32px

---

### 3.7 用料选材页

#### 章节标题
```css
className="text-2xl font-bold text-gray-900 mb-4 pb-2 border-b-2 border-orange-200"
```
- **字号**: 2xl (约24px)
- **字重**: bold
- **下边距**: 16px
- **底部内边距**: 8px
- **底部边框**: 2px，橙色200号

#### 用料网格
```css
className="grid md:grid-cols-2 gap-6"
```
- **布局**: 响应式2列网格
- **间距**: 24px

#### 用料卡片
```css
// 主料
className="bg-orange-50 rounded-lg p-5 border border-orange-200"

// 辅料
className="bg-green-50 rounded-lg p-5 border border-green-200"
```
- **内边距**: 20px
- **圆角**: lg

#### 选材指南卡片
```css
className="bg-blue-50 rounded-lg p-5 border-l-4 border-blue-500"
```
- **背景**: 浅色
- **左边框**: 4px加粗，作为视觉重点
- **内边距**: 20px

#### 提示框
```css
className="text-sm text-blue-700 bg-blue-100 rounded px-3 py-2"
```
- **字号**: sm (约14px)
- **内边距**: 水平12px，垂直8px

---

### 3.8 烹饪步骤页（时间轴设计）

#### 时间轴容器
```css
className="relative pl-12 pb-8 border-l-2 border-orange-200 last:border-l-0 last:pb-0"
```
- **左内边距**: 48px（为数字圆圈留空间）
- **底部内边距**: 32px
- **左边框**: 2px，连接线
- **最后一项**: 无边框和底部内边距

#### 步骤数字圆圈
```css
className="absolute -left-4 top-0 w-8 h-8 bg-orange-500 text-white rounded-full flex items-center justify-center font-bold shadow-md"
```
- **定位**: 绝对定位，left: -16px
- **尺寸**: 32x32px
- **背景**: 橙色500号
- **形状**: 圆形
- **布局**: flex居中

#### 步骤卡片
```css
className="bg-gray-50 rounded-lg p-5 hover:shadow-md transition-shadow"
```
- **背景**: 浅灰色
- **内边距**: 20px
- **悬停效果**: 阴影加深

#### 步骤标题行
```css
className="flex items-start justify-between mb-3"
```
- **布局**: 两端对齐
- **下边距**: 12px

#### 时间/火候标签
```css
// 火候
className="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-xs font-medium"

// 时间
className="px-3 py-1 bg-purple-100 text-purple-700 rounded-full text-xs font-medium"
```
- **字号**: xs (约12px)
- **形状**: 全圆角

#### 提示框
```css
className="bg-amber-50 border-l-4 border-amber-400 rounded p-3"
```
- **左边框**: 4px，琥珀色400号
- **内边距**: 12px

---

### 3.9 进阶技巧页

#### 网格布局
```css
className="grid md:grid-cols-2 gap-4"
```
- **布局**: 响应式2列
- **间距**: 16px

#### 技巧卡片
```css
className="bg-gradient-to-br from-blue-50 to-blue-100 rounded-lg p-5 border border-blue-200"
```
- **背景**: 渐变
- **内边距**: 20px
- **边框**: 1px

**四种技巧颜色**:
- 更清亮: blue系
- 更鲜: green系
- 气味管理: purple系
- 口感平衡: amber系

---

### 3.10 注意事项页

#### 通用提示
```css
className="bg-blue-50 rounded-lg p-4 border-l-4 border-blue-500"
```
- **左边框**: 4px
- **符号**: ✓ (蓝色text-blue-600)

#### 常见错误
```css
className="bg-red-50 rounded-lg p-4 border-l-4 border-red-500"
```
- **背景**: 红色50号
- **左边框**: 4px，红色
- **符号**: ✗ (红色text-red-600)

---

## 四、颜色系统

### 主色调
- **橙色系**: 主要强调色（按钮、标题边框、时间轴）
  - orange-50/100/200/500/700/900

### 功能色
- **蓝色系**: 信息、提示
  - blue-50/100/200/500/600/700/900
  
- **绿色系**: 成功、辅料
  - green-50/100/200/700/900
  
- **紫色系**: 次要信息
  - purple-50/100/200/700/900
  
- **琥珀色系**: 警告、提示
  - amber-50/100/200/400/700/900
  
- **红色系**: 错误、警告
  - red-50/200/500/600

### 中性色
- **灰色系**: 文字、背景
  - gray-50/600/700/900

---

## 五、间距系统

### Tailwind间距对应
- `p-2`: 8px
- `p-3`: 12px
- `p-4`: 16px
- `p-5`: 20px
- `p-6`: 24px
- `p-8`: 32px
- `gap-1`: 4px
- `gap-2`: 8px
- `gap-3`: 12px
- `gap-4`: 16px
- `gap-6`: 24px

---

## 六、响应式断点

### Tailwind断点
- `md:`: 768px及以上
  - 用于: 网格列数、卡片布局

### 当前使用
- 头部卡片: `grid-cols-1 md:grid-cols-4` → **建议改为**: `grid-cols-4`
- 内容网格: `md:grid-cols-2` (保持)

---

## 七、状态管理

### useState Hook
```javascript
const [activeTab, setActiveTab] = useState('ingredients');
```
- 初始值: 'ingredients'
- 四个可选值: 'ingredients', 'steps', 'tips', 'notes'

### 条件渲染
```javascript
{activeTab === 'ingredients' && <div>...</div>}
{activeTab === 'steps' && <div>...</div>}
{activeTab === 'tips' && <div>...</div>}
{activeTab === 'notes' && <div>...</div>}
```

---

## 八、数据映射规则

### 从JSON到UI的映射

#### 头部信息
```javascript
{
  "菜名": <h1>,
  "标签": <标签容器>,
  "简介": <p>,
  "难度": <卡片1>,
  "准备时间": <卡片2>,
  "烹饪时间": <卡片3>,
  "总时间": <卡片4>
}
```

#### 步骤数组
```javascript
recipe.烹饪流程.步骤顺序数组.map((step, idx) => (
  <步骤卡片 key={idx}>
    <数字圆圈>{idx + 1}</数字圆圈>
    <标题>{step.步骤名}</标题>
    <描述>{step.操作}</描述>
    <标签>{step.火候} {step.时间}</标签>
    <提示>{step.tips}</提示>
  </步骤卡片>
))
```

---

## 九、复现检查清单

### 必须包含的元素
- ✅ Tailwind CSS核心工具类
- ✅ lucide-react图标库
- ✅ React hooks (useState)
- ✅ 响应式网格布局
- ✅ 渐变背景和卡片
- ✅ 左边框强调设计
- ✅ 时间轴垂直布局
- ✅ 悬停效果 (hover:)
- ✅ 圆角变化 (rounded-lg/xl/full)
- ✅ 条件渲染标签页

### 关键修改点
**快速信息卡片改为一排显示**:
```css
修改前: className="grid grid-cols-1 md:grid-cols-4 gap-4 mt-6"
修改后: className="grid grid-cols-4 gap-3 mt-6"
```

---

## 十、扩展指南

### 如果需要添加图片
在头部区域添加:
```html
<img src="dish-image.jpg" alt="菜品图片" 
     className="w-full h-64 object-cover rounded-xl mb-4" />
```

### 如果需要打印样式
添加媒体查询:
```css
@media print {
  .no-print { display: none; }
  .bg-gradient-to-br { background: white; }
}
```

### 如果需要深色模式
在类名前添加 `dark:` 前缀:
```css
className="bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100"
```

---

## 十一、完整类名索引

### 常用组合
- **卡片容器**: `bg-[color]-50 rounded-lg p-5 border border-[color]-200`
- **强调边框**: `border-l-4 border-[color]-500`
- **章节标题**: `text-2xl font-bold text-gray-900 mb-4 pb-2 border-b-2 border-orange-200`
- **小标签**: `px-3 py-1 bg-[color]-100 text-[color]-700 rounded-full text-xs font-medium`
- **大标签**: `px-3 py-1 bg-orange-100 text-orange-700 rounded-full text-sm font-medium`

---

**文档版本**: 1.0  
**最后更新**: 2025-10-20  
**适用框架**: React + Tailwind CSS
